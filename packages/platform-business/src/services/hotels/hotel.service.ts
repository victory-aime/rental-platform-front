import { BaseApi } from '../../api'

/**
 * HotelServices provides methods to manage hotels.
 * It interacts with the API endpoints defined in the application context.
 */
export class HotelServices extends BaseApi {}
